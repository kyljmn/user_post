<h1>Welcome!</h1>
<% if user_signed_in? %>
  <%= link_to "New Post", new_user_post_path(current_user.id) %>
  <% if @posts.any? %>
    <h2>Posts</h2>
    <%= render partial: "posts/table", object: @posts %>
  <% end %>
<% end %>
